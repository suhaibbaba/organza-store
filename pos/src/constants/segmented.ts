// A little breathing room either side of the chosen segment when a tab row
// has to scroll it into view, so it never lands flush against the edge and
// reads as though it were cut off.
export const SEGMENT_SCROLL_PADDING_PX = 12;
