// Shared field limits used across Zod schemas.
export const PASSWORD_MIN_LENGTH = 8;

// Numbered shawls (spec.md): imageX/imageY are percentages, not pixels.
export const IMAGE_POINT_MIN = 0;
export const IMAGE_POINT_MAX = 100;
