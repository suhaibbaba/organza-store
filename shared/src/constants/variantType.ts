// The "Number/الأرقام" global variant type (seeded, see prisma/seed.ts) is
// reused as-is for numbered shawls (spec.md "Numbered shawls") — its option
// values double as the numbers placed on the product image. Admin UIs use
// this slug to detect a numbered-shawl-eligible product.
export const NUMBER_VARIANT_TYPE_SLUG = "number";
