// Mirrors the seed's mobile numbers (e.g. "+970599000001": prefix "599" +
// a 6-digit local number) so phones generated for tests fall in the same
// libphonenumber-js-validated Palestinian mobile range.
export const TEST_MOBILE_PREFIX = "599";
export const TEST_LOCAL_NUMBER_DIGITS = 6;
